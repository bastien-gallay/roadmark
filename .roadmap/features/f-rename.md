+++
id = "F-rename"
type = "feature"
class = "table-stakes"
effort = "M"
area = ["cli", "core"]
horizon = "shipped"
status = "done"
target = ["v0.4"]
shipped = { version = "v0.4.0", date = "2026-07-12" }
shipped_order = 7
+++

`roadmap rename`: rename a feature id, move its file, and rewrite cross-references so anchors stay consistent.
