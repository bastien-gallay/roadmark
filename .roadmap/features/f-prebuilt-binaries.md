+++
id = "F-prebuilt-binaries"
type = "chore"
effort = "M"
area = ["release"]
horizon = "shipped"
status = "done"
target = ["v0.1"]
shipped = { version = "v0.1.0", date = "2026-07-11" }
shipped_order = 4
+++

cargo-dist release pipeline: binaries for 5 targets plus shell/powershell installers on every `v<semver>` tag.
